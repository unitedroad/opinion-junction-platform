  <carousel id="carousel" interval="10000" oj-carousel>
    {% for article in articles %}
    <slide ng-repeat="" active="article.active">
      <img ng-src="{{article.header_image}}" style="margin:auto;">
      <div class="carousel-caption">
        <a ng-href="/article/{{article.id}}/{{article.slug}}" class="carousel-article-link"><h2>{{article.title}}</h2></a>
        <p><h5>{{article.headerCategory.friendly_name}}</h5></p>
        <p><h6>{{article.author.first_name}} {{article.author.last_name}}</h6></p>
      </div>
    </slide>
    {% endfor %}
  </carousel>
